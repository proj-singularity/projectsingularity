spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: varunpanyam
    password: varunpanyam
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
  mail:
    host: smtp.gmail.com
    port: 587
    username: varunpanyam
    password: varunpanyam
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


projectsingularity.app.jwtSecret: secret
projectsingularity.app.jwtExpirationMs: 86400000

server:
  port: 8081
